function favoriteIceCream(flavor) {
  return 'I love ' + flavor;
}

const theTruth = () => true;

const sayHey = () => 'Hey!';

const sayHeyFriend = name => 'Hey, ' + name + '!';

function shouting(string) {
  return string.toUpperCase();
}

const roundDown = (num) => Math.floor(num);